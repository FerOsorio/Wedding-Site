<div class="container">
	<nav>
		<ul>
			<% for (var i=0; i < items.length ; i++){ %>
				<% if (items[i].display === false) continue; %>
				<li class="menuItem">
				<a class="menuItem" href="#<%= items[i].name %>"><%= items[i].displayName %></a>
				</li>
			<% } %>
		</ul>
	</nav>
</div>